# Source

Put the organelle builder module here while developing. When ready, promote the
module into `src/CellExperience/OrganelleModels/` and register it in
`src/CellExperience/OrganelleRegistry.lua`.

